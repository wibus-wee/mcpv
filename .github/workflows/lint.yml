name: Lint

on:
  workflow_call:
  workflow_dispatch: {}

permissions:
  contents: read
  pull-requests: read  # Required for only-new-issues option

jobs:
  go-lint:
    name: Go Lint
    uses: ./.github/workflows/linters/go-lint.yml

  web-lint:
    name: Web Lint
    uses: ./.github/workflows/linters/web-lint.yml
